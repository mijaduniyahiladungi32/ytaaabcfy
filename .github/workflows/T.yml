name: Generate Playlist

on:
  workflow_dispatch:  # Manual trigger
  # Runs every 6 hours (adjust as needed)
  push:
    branches:
      - main  # Or your default branch

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: write   # Allow committing back to repo

    steps:
      # Checkout your repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # Setup Python
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # Install dependencies
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install playwright requests pytz
          playwright install chromium --with-deps

      # Run your Python script
      - name: Run playlist generator
        run: |
          python t.py  # ⬅️ Replace with your actual filename

      # Commit and push the updated playlist.m3u file
      - name: Commit and push playlist
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add	.
          git commit -m "Auto update playlist.m3u [skip ci]" || echo "No changes to commit"
          git data-capture
